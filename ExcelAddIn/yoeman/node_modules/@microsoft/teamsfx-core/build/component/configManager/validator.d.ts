import { ValidateFunction } from "ajv";
import { YAMLDiagnostics } from "./diagnostic";
declare type Version = string;
export declare class Validator {
    impl: Map<Version, {
        validator: ValidateFunction;
        yamlDiagnostics: YAMLDiagnostics;
    }>;
    constructor();
    private initVersion;
    isVersionSupported(version: string): boolean;
    supportedVersions(): string[];
    private latestSupportedVersion;
    generateDiagnosticMessage(yamlPath: string, yamlString: string, version?: string): Promise<string | undefined>;
    validate(obj: Record<string, unknown>, version?: string): boolean;
}
export {};
//# sourceMappingURL=validator.d.ts.map