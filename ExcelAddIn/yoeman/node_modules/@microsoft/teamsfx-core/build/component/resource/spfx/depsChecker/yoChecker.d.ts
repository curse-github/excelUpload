import { ContextV3, FxError, LogProvider, PluginContext, Result } from "@microsoft/teamsfx-api";
import { DependencyChecker, DependencyInfo } from "./dependencyChecker";
export declare class YoChecker implements DependencyChecker {
    private readonly _logger;
    constructor(logger: LogProvider);
    static getDependencyInfo(): DependencyInfo;
    ensureDependency(ctx: PluginContext | ContextV3): Promise<Result<boolean, FxError>>;
    ensureLatestDependency(ctx: PluginContext | ContextV3): Promise<Result<boolean, FxError>>;
    isLatestInstalled(): Promise<boolean>;
    isInstalled(): Promise<boolean>;
    install(): Promise<void>;
    getBinFolders(): Promise<string[]>;
    findGloballyInstalledVersion(timeoutInSeconds?: number): Promise<string | undefined>;
    findLatestVersion(timeoutInSeconds: number): Promise<string | undefined>;
    private validate;
    private getDefaultInstallPath;
    private getSentinelPath;
    private queryVersion;
    private cleanup;
    private installYo;
}
//# sourceMappingURL=yoChecker.d.ts.map